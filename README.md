# lq-diffusion-classic
An implementation of time discrete denoiser diffusion model in pytorch made for those seeking a better understanding of DMs applied specifically to experimental images of liquid crytal's textures. 
One can change the dataset to be trained on freely by changing the config.json file. The conditional training can also be turned off by changing label_dim=0 inside config file.
